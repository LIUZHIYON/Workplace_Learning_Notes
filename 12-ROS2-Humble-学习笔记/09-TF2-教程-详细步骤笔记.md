# ROS2 Humble TF2 教程 — 分节详细步骤笔记

> 来源：https://docs.ros.org/en/humble/Tutorials/Intermediate/Tf2/Tf2-Main.html
>
> 本笔记针对每一小节，以"我看不懂，请告诉我改什么文件、在哪个目录执行什么命令"的视角，逐条拆解。

---

## 目录

1. [准备工作：创建工作空间](#1-准备工作创建工作空间)
2. [第1节：Introduction to tf2（入门演示）](#2-第1节introduction-to-tf2入门演示)
3. [第2节：Writing a static broadcaster (Python)](#3-第2节writing-a-static-broadcaster-python)
4. [第3节：Writing a broadcaster (Python)](#4-第3节writing-a-broadcaster-python)
5. [第4节：Writing a listener (Python)](#5-第4节writing-a-listener-python)
6. [第5节：Adding a frame (Python)](#6-第5节adding-a-frame-python)
7. [第6节：Using time (C++)](#7-第6节using-time-c)
8. [第7节：Traveling in time (C++)](#8-第7节traveling-in-time-c)
9. [第8节：Debugging tf2 problems](#9-第8节debugging-tf2-problems)
10. [第9节：Quaternion fundamentals](#10-第9节quaternion-fundamentals)
11. [第10节：Using stamped datatypes with tf2_ros::MessageFilter](#11-第10节using-stamped-datatypes-with-tf2_rosmessagefilter)

---

## 1. 准备工作：创建工作空间

> **如果你还没有创建工作空间，请先完成这一步。**

### 1.1 创建目录结构

打开终端（Linux/macOS 用 bash，Windows 用 x64 Native Tools Command Prompt 或 PowerShell），执行：

```bash
# Linux / macOS
mkdir -p ~/ros2_ws/src
cd ~/ros2_ws/src

# Windows (cmd)
md \ros2_ws\src
cd \ros2_ws\src
```

### 1.2 克隆示例仓库

在 `~/ros2_ws/src`（或 `\ros2_ws\src`）目录下执行：

```bash
git clone https://github.com/ros/ros_tutorials.git -b humble
```

这会下载 `turtlesim` 等包。

### 1.3 检查依赖并编译

回到工作空间根目录：

```bash
# Linux
cd ~/ros2_ws
rosdep install -i --from-path src --rosdistro humble -y

# Windows/macOS: rosdep 只在 Linux 上运行，跳过即可
```

编译：

```bash
# Linux / macOS
cd ~/ros2_ws
colcon build

# Windows
cd \ros2_ws
colcon build --merge-install
```

### 1.4 配置 overlay 环境

打开**新终端**，执行：

```bash
# Linux
source /opt/ros/humble/setup.bash
cd ~/ros2_ws
source install/local_setup.bash

# Windows (cmd)
call C:\dev\ros2\local_setup.bat
cd \ros2_ws
call install\setup.bat

# Windows (PowerShell)
C:\dev\ros2\local_setup.ps1
cd \ros2_ws
.\install\setup.ps1
```

验证：

```bash
ros2 run turtlesim turtlesim_node
```

能看到海龟窗口说明工作空间正常。

---

## 2. 第1节：Introduction to tf2（入门演示）

> 目标：运行 turtlesim 演示，感受 tf2 的能力。

### 2.1 安装 demo 包

打开终端，执行以下命令安装所需包：

```bash
# Ubuntu
sudo apt install ros-humble-rviz2 ros-humble-turtle-tf2-py ros-humble-tf2-ros ros-humble-tf2-tools ros-humble-turtlesim

# RHEL
sudo dnf install ros-humble-rviz2 ros-humble-turtle-tf2-py ros-humble-tf2-ros ros-humble-tf2-tools ros-humble-turtlesim
```

### 2.2 运行 demo

打开第一个终端，source ROS2 环境，然后：

```bash
# source 环境（根据你的安装方式调整）
source /opt/ros/humble/setup.bash

# 启动 demo
ros2 launch turtle_tf2_py turtle_tf2_demo.launch.py
```

这时你会看到 turtlesim 窗口出现两只海龟。

### 2.3 启动键盘控制

打开**第二个终端**，执行：

```bash
source /opt/ros/humble/setup.bash
ros2 run turtlesim turtle_teleop_key
```

用键盘方向键控制中间那只海龟，你会看到另一只海龟始终跟着它。

### 2.4 使用 tf2 工具

#### 2.4.1 view_frames — 查看坐标帧树

```bash
ros2 run tf2_tools view_frames
```

等待 5 秒，会生成 `frames.pdf` 文件，用 PDF 查看器打开可以看到 `world` → `turtle1` / `turtle2` 的坐标帧树。

> ⚠ Windows 不支持 `view_frames`，可以直接跳到 `tf2_echo`。

#### 2.4.2 tf2_echo — 查看帧之间的变换关系

```bash
ros2 run tf2_ros tf2_echo turtle2 turtle1
```

会实时显示 `turtle2` 相对于 `turtle1` 的位置和旋转（平移、四元数、RPY 欧拉角、变换矩阵）。用方向键开车时数据会变化。

### 2.5 用 rviz2 可视化

```bash
# Linux
ros2 run rviz2 rviz2 -d $(ros2 pkg prefix --share turtle_tf2_py)/rviz/turtle_rviz.rviz

# Windows (cmd)
for /f "usebackq tokens=*" %a in (`ros2 pkg prefix --share turtle_tf2_py`) do rviz2 -d %a/rviz/turtle_rviz.rviz
```

打开后可以看到坐标帧的 3D 可视化。

---

## 3. 第2节：Writing a static broadcaster (Python)

> 目标：编写一个节点，广播静态坐标变换（不会随时间变化的帧）。

### 3.1 创建包

打开终端，source 环境后，进入工作空间的 src 目录：

```bash
source /opt/ros/humble/setup.bash
cd ~/ros2_ws/src
```

创建 Python 包：

```bash
ros2 pkg create --build-type ament_python --license Apache-2.0 -- learning_tf2_py
```

终端会返回成功消息。

### 3.2 写入静态广播器节点代码

进入包的源码目录：

```bash
cd ~/ros2_ws/src/learning_tf2_py/learning_tf2_py
```

下载示例代码：

```bash
# Linux / macOS
wget https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/static_turtle_tf2_broadcaster.py

# Windows (cmd)
curl -sk https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/static_turtle_tf2_broadcaster.py -o static_turtle_tf2_broadcaster.py

# Windows (PowerShell)
curl https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/static_turtle_tf2_broadcaster.py -o static_turtle_tf2_broadcaster.py
```

用文本编辑器打开 `static_turtle_tf2_broadcaster.py`，代码内容如下（可以直接粘贴）：

```python
import math
import sys

from geometry_msgs.msg import TransformStamped

import numpy as np

import rclpy
from rclpy.node import Node

from tf2_ros.static_transform_broadcaster import StaticTransformBroadcaster


def quaternion_from_euler(ai, aj, ak):
    ai /= 2.0
    aj /= 2.0
    ak /= 2.0
    ci = math.cos(ai)
    si = math.sin(ai)
    cj = math.cos(aj)
    sj = math.sin(aj)
    ck = math.cos(ak)
    sk = math.sin(ak)
    cc = ci*ck
    cs = ci*sk
    sc = si*ck
    ss = si*sk

    q = np.empty((4, ))
    q[0] = cj*sc - sj*cs
    q[1] = cj*ss + sj*cc
    q[2] = cj*cs - sj*sc
    q[3] = cj*cc + sj*ss

    return q


class StaticFramePublisher(Node):
    def __init__(self, transformation):
        super().__init__('static_turtle_tf2_broadcaster')
        self.tf_static_broadcaster = StaticTransformBroadcaster(self)
        self.make_transforms(transformation)

    def make_transforms(self, transformation):
        t = TransformStamped()

        t.header.stamp = self.get_clock().now().to_msg()
        t.header.frame_id = 'world'
        t.child_frame_id = transformation[1]

        t.transform.translation.x = float(transformation[2])
        t.transform.translation.y = float(transformation[3])
        t.transform.translation.z = float(transformation[4])
        quat = quaternion_from_euler(
            float(transformation[5]), float(transformation[6]), float(transformation[7]))
        t.transform.rotation.x = quat[0]
        t.transform.rotation.y = quat[1]
        t.transform.rotation.z = quat[2]
        t.transform.rotation.w = quat[3]

        self.tf_static_broadcaster.sendTransform(t)


def main():
    logger = rclpy.logging.get_logger('logger')
    if len(sys.argv) != 8:
        logger.info('Invalid number of parameters. Usage: \n'
                    '$ ros2 run learning_tf2_py static_turtle_tf2_broadcaster'
                    'child_frame_name x y z roll pitch yaw')
        sys.exit(1)

    if sys.argv[1] == 'world':
        logger.info('Your static turtle name cannot be "world"')
        sys.exit(2)

    rclpy.init()
    node = StaticFramePublisher(sys.argv)
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    rclpy.shutdown()
```

### 3.3 修改 package.xml

回到包根目录：

```bash
cd ~/ros2_ws/src/learning_tf2_py
```

用文本编辑器打开 `package.xml`，修改：

```xml
<description>Learning tf2 with rclpy</description>
<maintainer email="you@email.com">Your Name</maintainer>
<license>Apache License 2.0</license>
```

然后在后面添加依赖：

```xml
<exec_depend>geometry_msgs</exec_depend>
<exec_depend>python3-numpy</exec_depend>
<exec_depend>rclpy</exec_depend>
<exec_depend>tf2_ros_py</exec_depend>
<exec_depend>turtlesim</exec_depend>
```

保存。

### 3.4 修改 setup.py — 添加入口点

用文本编辑器打开 `~/ros2_ws/src/learning_tf2_py/setup.py`。

在 `console_scripts` 的花括号里添加一行：

```python
'static_turtle_tf2_broadcaster = learning_tf2_py.static_turtle_tf2_broadcaster:main',
```

修改后类似：

```python
entry_points={
    'console_scripts': [
        'static_turtle_tf2_broadcaster = learning_tf2_py.static_turtle_tf2_broadcaster:main',
    ],
},
```

保存。

### 3.5 编译

回到工作空间根目录：

```bash
cd ~/ros2_ws
```

```bash
# Linux / macOS
colcon build --packages-select learning_tf2_py

# Windows
colcon build --merge-install --packages-select learning_tf2_py
```

### 3.6 配置环境

打开**新终端**：

```bash
source /opt/ros/humble/setup.bash
cd ~/ros2_ws

# Linux / macOS
. install/setup.bash

# Windows (cmd)
call install\setup.bat

# Windows (PowerShell)
.\install\setup.ps1
```

### 3.7 运行

```bash
ros2 run learning_tf2_py static_turtle_tf2_broadcaster mystaticturtle 0 0 1 0 0 0
```

**参数解释**：`mystaticturtle`（子帧名） `x=0` `y=0` `z=1`（往上1米） `roll=0` `pitch=0` `yaw=0`（无旋转）。

### 3.8 验证

```bash
ros2 topic echo /tf_static
```

应该看到输出类似：

```
transforms:
- header:
    stamp: ...
    frame_id: world
  child_frame_id: mystaticturtle
  transform:
    translation:
      x: 0.0
      y: 0.0
      z: 1.0
    rotation:
      x: 0.0
      y: 0.0
      z: 0.0
      w: 1.0
```

### 3.9 用官方工具发布静态变换（不用写代码）

如果想省事，直接用 `static_transform_publisher`：

```bash
ros2 run tf2_ros static_transform_publisher --x 0 --y 0 --z 1 --yaw 0 --pitch 0 --roll 0 --frame-id world --child-frame-id mystaticturtle
```

或者用四元数：

```bash
ros2 run tf2_ros static_transform_publisher --x 0 --y 0 --z 1 --qx 0 --qy 0 --qz 0 --qw 1 --frame-id world --child-frame-id mystaticturtle
```

这个工具也可以写在 launch 文件里使用。

---

## 4. 第3节：Writing a broadcaster (Python)

> 目标：编写一个节点，广播移动中机器人的位姿到 tf2。

### 4.1 写入广播器节点代码

先进入包的源码目录：

```bash
cd ~/ros2_ws/src/learning_tf2_py/learning_tf2_py
```

下载示例代码：

```bash
# Linux / macOS
wget https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/turtle_tf2_broadcaster.py

# Windows (cmd)
curl -sk https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/turtle_tf2_broadcaster.py -o turtle_tf2_broadcaster.py

# Windows (PowerShell)
curl https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/turtle_tf2_broadcaster.py -o turtle_tf2_broadcaster.py
```

用文本编辑器打开 `turtle_tf2_broadcaster.py`，内容如下：

```python
import math

from geometry_msgs.msg import TransformStamped

import numpy as np

import rclpy
from rclpy.node import Node

from tf2_ros import TransformBroadcaster

from turtlesim.msg import Pose


def quaternion_from_euler(ai, aj, ak):
    ai /= 2.0
    aj /= 2.0
    ak /= 2.0
    ci = math.cos(ai)
    si = math.sin(ai)
    cj = math.cos(aj)
    sj = math.sin(aj)
    ck = math.cos(ak)
    sk = math.sin(ak)
    cc = ci*ck
    cs = ci*sk
    sc = si*ck
    ss = si*sk

    q = np.empty((4, ))
    q[0] = cj*sc - sj*cs
    q[1] = cj*ss + sj*cc
    q[2] = cj*cs - sj*sc
    q[3] = cj*cc + sj*ss

    return q


class FramePublisher(Node):
    def __init__(self):
        super().__init__('turtle_tf2_frame_publisher')
        self.turtlename = self.declare_parameter(
            'turtlename', 'turtle').get_parameter_value().string_value
        self.tf_broadcaster = TransformBroadcaster(self)
        self.subscription = self.create_subscription(
            Pose,
            f'/{self.turtlename}/pose',
            self.handle_turtle_pose,
            1)
        self.subscription

    def handle_turtle_pose(self, msg):
        t = TransformStamped()
        t.header.stamp = self.get_clock().now().to_msg()
        t.header.frame_id = 'world'
        t.child_frame_id = self.turtlename

        t.transform.translation.x = msg.x
        t.transform.translation.y = msg.y
        t.transform.translation.z = 0.0

        q = quaternion_from_euler(0, 0, msg.theta)
        t.transform.rotation.x = q[0]
        t.transform.rotation.y = q[1]
        t.transform.rotation.z = q[2]
        t.transform.rotation.w = q[3]

        self.tf_broadcaster.sendTransform(t)


def main():
    rclpy.init()
    node = FramePublisher()
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    rclpy.shutdown()
```

### 4.2 修改 setup.py — 添加入口点

编辑 `~/ros2_ws/src/learning_tf2_py/setup.py`，在 `console_scripts` 中添加：

```python
'turtle_tf2_broadcaster = learning_tf2_py.turtle_tf2_broadcaster:main',
```

修改后：

```python
entry_points={
    'console_scripts': [
        'static_turtle_tf2_broadcaster = learning_tf2_py.static_turtle_tf2_broadcaster:main',
        'turtle_tf2_broadcaster = learning_tf2_py.turtle_tf2_broadcaster:main',
    ],
},
```

### 4.3 创建 launch 文件

```bash
mkdir -p ~/ros2_ws/src/learning_tf2_py/launch
```

在 `~/ros2_ws/src/learning_tf2_py/launch/` 下创建 `turtle_tf2_demo_launch.py`：

```python
from launch import LaunchDescription
from launch_ros.actions import Node

def generate_launch_description():
    return LaunchDescription([
        Node(
            package='turtlesim',
            executable='turtlesim_node',
            name='sim'
        ),
        Node(
            package='learning_tf2_py',
            executable='turtle_tf2_broadcaster',
            name='broadcaster1',
            parameters=[
                {'turtlename': 'turtle1'}
            ]
        ),
    ])
```

### 4.4 修改 package.xml — 添加 launch 依赖

编辑 `package.xml`，添加：

```xml
<exec_depend>launch</exec_depend>
<exec_depend>launch_ros</exec_depend>
```

### 4.5 修改 setup.py — 添加 data_files 使 launch 文件被安装

编辑 `setup.py`，顶部添加 import：

```python
import os
from glob import glob
```

在 `data_files` 中添加：

```python
data_files=[
    ...
    (os.path.join('share', package_name, 'launch'), glob('launch/*')),
],
```

### 4.6 编译

```bash
cd ~/ros2_ws
colcon build --packages-select learning_tf2_py
# Windows: colcon build --merge-install --packages-select learning_tf2_py
```

### 4.7 运行

打开**新终端**，source 环境：

```bash
source /opt/ros/humble/setup.bash
cd ~/ros2_ws
. install/setup.bash   # Linux/macOS
```

运行 launch 文件：

```bash
ros2 launch learning_tf2_py turtle_tf2_demo_launch.py
```

打开**另一个终端**启动键盘控制：

```bash
source /opt/ros/humble/setup.bash
ros2 run turtlesim turtle_teleop_key
```

### 4.8 验证

在第三个终端里用 `tf2_echo` 检查：

```bash
source /opt/ros/humble/setup.bash
ros2 run tf2_ros tf2_echo world turtle1
```

会显示 turtle1 的位置和旋转数据。开车时数值会变化。

---

## 5. 第4节：Writing a listener (Python)

> 目标：编写 tf2 监听器，获取帧变换信息并控制第二只海龟跟随第一只。

### 5.1 写入监听器节点代码

```bash
cd ~/ros2_ws/src/learning_tf2_py/learning_tf2_py
```

下载示例：

```bash
# Linux / macOS
wget https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/turtle_tf2_listener.py

# Windows (cmd)
curl -sk https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/turtle_tf2_listener.py -o turtle_tf2_listener.py

# Windows (PowerShell)
curl https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/turtle_tf2_listener.py -o turtle_tf2_listener.py
```

打开 `turtle_tf2_listener.py`，内容：

```python
import math

from geometry_msgs.msg import Twist

import rclpy
from rclpy.node import Node

from tf2_ros import TransformException
from tf2_ros.buffer import Buffer
from tf2_ros.transform_listener import TransformListener

from turtlesim.srv import Spawn


class FrameListener(Node):
    def __init__(self):
        super().__init__('turtle_tf2_frame_listener')
        self.target_frame = self.declare_parameter(
            'target_frame', 'turtle1').get_parameter_value().string_value

        self.tf_buffer = Buffer()
        self.tf_listener = TransformListener(self.tf_buffer, self)

        self.spawner = self.create_client(Spawn, 'spawn')
        self.turtle_spawning_service_ready = False
        self.turtle_spawned = False

        self.publisher = self.create_publisher(Twist, 'turtle2/cmd_vel', 1)
        self.timer = self.create_timer(1.0, self.on_timer)

    def on_timer(self):
        from_frame_rel = self.target_frame
        to_frame_rel = 'turtle2'

        if self.turtle_spawning_service_ready:
            if self.turtle_spawned:
                try:
                    t = self.tf_buffer.lookup_transform(
                        to_frame_rel,
                        from_frame_rel,
                        rclpy.time.Time())
                except TransformException as ex:
                    self.get_logger().info(
                        f'Could not transform {to_frame_rel} to {from_frame_rel}: {ex}')
                    return

                msg = Twist()
                scale_rotation_rate = 1.0
                msg.angular.z = scale_rotation_rate * math.atan2(
                    t.transform.translation.y,
                    t.transform.translation.x)

                scale_forward_speed = 0.5
                msg.linear.x = scale_forward_speed * math.sqrt(
                    t.transform.translation.x ** 2 +
                    t.transform.translation.y ** 2)

                self.publisher.publish(msg)
            else:
                if self.result.done():
                    self.get_logger().info(
                        f'Successfully spawned {self.result.result().name}')
                    self.turtle_spawned = True
                else:
                    self.get_logger().info('Spawn is not finished')
        else:
            if self.spawner.service_is_ready():
                request = Spawn.Request()
                request.name = 'turtle2'
                request.x = float(4)
                request.y = float(2)
                request.theta = float(0)
                self.result = self.spawner.call_async(request)
                self.turtle_spawning_service_ready = True
            else:
                self.get_logger().info('Service is not ready')


def main():
    rclpy.init()
    node = FrameListener()
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    rclpy.shutdown()
```

### 5.2 修改 setup.py — 添加入口点

编辑 `setup.py`，在 `console_scripts` 中添加：

```python
'turtle_tf2_listener = learning_tf2_py.turtle_tf2_listener:main',
```

### 5.3 更新 launch 文件

编辑 `~/ros2_ws/src/learning_tf2_py/launch/turtle_tf2_demo_launch.py`，替换为：

```python
from launch import LaunchDescription
from launch_ros.actions import Node
from launch.actions import DeclareLaunchArgument
from launch.substitutions import LaunchConfiguration

def generate_launch_description():
    return LaunchDescription([
        DeclareLaunchArgument(
            'target_frame', default_value='turtle1',
            description='Target frame name'
        ),
        Node(
            package='turtlesim',
            executable='turtlesim_node',
            name='sim'
        ),
        Node(
            package='learning_tf2_py',
            executable='turtle_tf2_broadcaster',
            name='broadcaster1',
            parameters=[
                {'turtlename': 'turtle1'}
            ]
        ),
        Node(
            package='learning_tf2_py',
            executable='turtle_tf2_broadcaster',
            name='broadcaster2',
            parameters=[
                {'turtlename': 'turtle2'}
            ]
        ),
        Node(
            package='learning_tf2_py',
            executable='turtle_tf2_listener',
            name='listener',
            parameters=[
                {'target_frame': LaunchConfiguration('target_frame')}
            ]
        ),
    ])
```

### 5.4 编译运行

```bash
cd ~/ros2_ws
colcon build --packages-select learning_tf2_py
# Windows: colcon build --merge-install --packages-select learning_tf2_py
```

新终端 source 后运行：

```bash
ros2 launch learning_tf2_py turtle_tf2_demo_launch.py
```

再开一个终端：

```bash
ros2 run turtlesim turtle_teleop_key
```

此时用方向键控制 turtle1，就会看到 turtle2 跟着 turtle1 移动。

---

## 6. 第5节：Adding a frame (Python)

> 目标：在变换树中添加固定帧和动态帧。

### 6.1 写入固定帧广播器节点

```bash
cd ~/ros2_ws/src/learning_tf2_py/learning_tf2_py
```

下载：

```bash
# Linux / macOS
wget https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/fixed_frame_tf2_broadcaster.py

# Windows (cmd)
curl -sk https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/fixed_frame_tf2_broadcaster.py -o fixed_frame_tf2_broadcaster.py

# Windows (PowerShell)
curl https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/fixed_frame_tf2_broadcaster.py -o fixed_frame_tf2_broadcaster.py
```

打开 `fixed_frame_tf2_broadcaster.py`：

```python
from geometry_msgs.msg import TransformStamped

import rclpy
from rclpy.node import Node

from tf2_ros import TransformBroadcaster


class FixedFrameBroadcaster(Node):
    def __init__(self):
        super().__init__('fixed_frame_tf2_broadcaster')
        self.tf_broadcaster = TransformBroadcaster(self)
        self.timer = self.create_timer(0.1, self.broadcast_timer_callback)

    def broadcast_timer_callback(self):
        t = TransformStamped()
        t.header.stamp = self.get_clock().now().to_msg()
        t.header.frame_id = 'turtle1'
        t.child_frame_id = 'carrot1'
        t.transform.translation.x = 0.0
        t.transform.translation.y = 2.0
        t.transform.translation.z = 0.0
        t.transform.rotation.x = 0.0
        t.transform.rotation.y = 0.0
        t.transform.rotation.z = 0.0
        t.transform.rotation.w = 1.0

        self.tf_broadcaster.sendTransform(t)


def main():
    rclpy.init()
    node = FixedFrameBroadcaster()
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    rclpy.shutdown()
```

### 6.2 写入动态帧广播器节点

```bash
# Linux / macOS
wget https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/dynamic_frame_tf2_broadcaster.py

# Windows (cmd)
curl -sk https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/dynamic_frame_tf2_broadcaster.py -o dynamic_frame_tf2_broadcaster.py

# Windows (PowerShell)
curl https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/dynamic_frame_tf2_broadcaster.py -o dynamic_frame_tf2_broadcaster.py
```

打开 `dynamic_frame_tf2_broadcaster.py`：

```python
import math

from geometry_msgs.msg import TransformStamped

import rclpy
from rclpy.node import Node

from tf2_ros import TransformBroadcaster


class DynamicFrameBroadcaster(Node):
    def __init__(self):
        super().__init__('dynamic_frame_tf2_broadcaster')
        self.tf_broadcaster = TransformBroadcaster(self)
        self.timer = self.create_timer(0.1, self.broadcast_timer_callback)

    def broadcast_timer_callback(self):
        seconds, _ = self.get_clock().now().seconds_nanoseconds()
        x = seconds * math.pi

        t = TransformStamped()
        t.header.stamp = self.get_clock().now().to_msg()
        t.header.frame_id = 'turtle1'
        t.child_frame_id = 'carrot1'
        t.transform.translation.x = 10 * math.sin(x)
        t.transform.translation.y = 10 * math.cos(x)
        t.transform.translation.z = 0.0
        t.transform.rotation.x = 0.0
        t.transform.rotation.y = 0.0
        t.transform.rotation.z = 0.0
        t.transform.rotation.w = 1.0

        self.tf_broadcaster.sendTransform(t)


def main():
    rclpy.init()
    node = DynamicFrameBroadcaster()
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    rclpy.shutdown()
```

### 6.3 修改 setup.py

编辑 `setup.py`，在 `console_scripts` 中添加：

```python
'fixed_frame_tf2_broadcaster = learning_tf2_py.fixed_frame_tf2_broadcaster:main',
'dynamic_frame_tf2_broadcaster = learning_tf2_py.dynamic_frame_tf2_broadcaster:main',
```

### 6.4 创建固定帧 launch 文件

创建 `~/ros2_ws/src/learning_tf2_py/launch/turtle_tf2_fixed_frame_demo_launch.py`：

```python
from launch import LaunchDescription
from launch_ros.actions import Node
from launch.actions import IncludeLaunchDescription
from launch.launch_description_sources import PythonLaunchDescriptionSource
from ament_index_python.packages import get_package_share_directory
import os

def generate_launch_description():
    demo_launch = IncludeLaunchDescription(
        PythonLaunchDescriptionSource([
            os.path.join(get_package_share_directory('learning_tf2_py'),
                         'launch', 'turtle_tf2_demo_launch.py')
        ]),
        launch_arguments={'target_frame': 'carrot1'}.items(),
    )

    return LaunchDescription([
        demo_launch,
        Node(
            package='learning_tf2_py',
            executable='fixed_frame_tf2_broadcaster',
            name='fixed_frame_tf2_broadcaster'
        ),
    ])
```

### 6.5 创建动态帧 launch 文件

创建 `~/ros2_ws/src/learning_tf2_py/launch/turtle_tf2_dynamic_frame_demo_launch.py`：

```python
from launch import LaunchDescription
from launch_ros.actions import Node
from launch.actions import IncludeLaunchDescription
from launch.launch_description_sources import PythonLaunchDescriptionSource
from ament_index_python.packages import get_package_share_directory
import os

def generate_launch_description():
    demo_launch = IncludeLaunchDescription(
        PythonLaunchDescriptionSource([
            os.path.join(get_package_share_directory('learning_tf2_py'),
                         'launch', 'turtle_tf2_demo_launch.py')
        ]),
        launch_arguments={'target_frame': 'carrot1'}.items(),
    )

    return LaunchDescription([
        demo_launch,
        Node(
            package='learning_tf2_py',
            executable='dynamic_frame_tf2_broadcaster',
            name='dynamic_frame_tf2_broadcaster'
        ),
    ])
```

### 6.6 确认 setup.py 的 data_files 包含 launch

确认 `setup.py` 中已经有：

```python
import os
from glob import glob

data_files=[
    ...
    (os.path.join('share', package_name, 'launch'), glob('launch/*')),
],
```

### 6.7 编译运行

```bash
cd ~/ros2_ws
colcon build --packages-select learning_tf2_py
# Windows: colcon build --merge-install --packages-select learning_tf2_py
```

source 后：

#### 运行固定帧 demo

```bash
ros2 launch learning_tf2_py turtle_tf2_fixed_frame_demo_launch.py
```

再开一个终端开车：

```bash
ros2 run turtlesim turtle_teleop_key
```

turtle2 会跟随 `carrot1` 而非直接跟 `turtle1`。

在启动时通过命令行指定目标帧：

```bash
ros2 launch learning_tf2_py turtle_tf2_fixed_frame_demo_launch.py target_frame:=carrot1
```

#### 运行动态帧 demo

```bash
ros2 launch learning_tf2_py turtle_tf2_dynamic_frame_demo_launch.py
```

`carrot1` 会围绕 `turtle1` 做圆周运动，turtle2 会追逐动态的 `carrot1`。

---

## 7. 第6节：Using time (C++)

> 目标：学习使用 `lookupTransform()` 的 timeout 参数来等待变换可用。

**注意：本节使用 C++，需要 C++ 包。**

### 7.1 创建 C++ 包（如果还没创建）

```bash
cd ~/ros2_ws/src
ros2 pkg create --build-type ament_cmake --license Apache-2.0 -- learning_tf2_cpp
```

然后按照前面的类似流程（broadcaster、listener 的 C++ 版本），把各节点加到 `learning_tf2_cpp` 包中。

### 7.2 修改 listener 节点 — 添加时间参数

打开 `src/learning_tf2_cpp/src/turtle_tf2_listener.cpp`。

找到 `lookupTransform()` 调用：

```cpp
t = tf_buffer_->lookupTransform(
    toFrameRel,
    fromFrameRel,
    tf2::TimePointZero);
```

改为：

```cpp
rclcpp::Time now = this->get_clock()->now();
t = tf_buffer_->lookupTransform(
    toFrameRel,
    fromFrameRel,
    now);
```

### 7.3 编译运行并查看错误

```bash
cd ~/ros2_ws
colcon build --packages-select learning_tf2_cpp
# Windows: colcon build --merge-install --packages-select learning_tf2_cpp
```

运行：

```bash
ros2 launch learning_tf2_cpp turtle_tf2_demo_launch.xml
```

你会看到类似错误：

```
[listener]: Could not transform turtle2 to turtle1: Lookup would
require extrapolation into the future...
```

这是因为请求 "现在" 的变换时，数据还没传到 buffer。

### 7.4 修复 — 添加 timeout 参数

再次编辑 `turtle_tf2_listener.cpp`，改为：

```cpp
rclcpp::Time now = this->get_clock()->now();
t = tf_buffer_->lookupTransform(
    toFrameRel,
    fromFrameRel,
    now,
    50ms);
```

第4个参数 `50ms` 表示最多等 50 毫秒，如果变换还不可用就抛异常。

### 7.5 重新编译运行

```bash
cd ~/ros2_ws
colcon build --packages-select learning_tf2_cpp
# 重新运行 launch，现在应该正常工作了
```

---

## 8. 第7节：Traveling in time (C++)

> 目标：让 turtle2 追随 5 秒前 turtle1 的位置（时空回溯）。

### 8.1 修改 lookupTransform — 回溯 5 秒

打开 `src/learning_tf2_cpp/src/turtle_tf2_listener.cpp`，修改 `lookupTransform()` 调用：

```cpp
rclcpp::Time when = this->get_clock()->now() - rclcpp::Duration(5, 0);
t = tf_buffer_->lookupTransform(
    toFrameRel,
    fromFrameRel,
    when,
    50ms);
```

### 8.2 运行并观察问题

```bash
cd ~/ros2_ws
colcon build --packages-select learning_tf2_cpp
ros2 launch learning_tf2_cpp turtle_tf2_fixed_frame_demo_launch.xml
```

前 5 秒钟 turtle2 不会动（没有 5 秒前的数据）。5 秒后 turtle2 会开始乱跑。

**原因**：我们问的是"5秒前 carrot1 相对于 5秒前 turtle2 的位置"——两个都是过去的数据。

### 8.3 使用高级 API 修复

修改 `lookupTransform()` 为六参数版本：

```cpp
rclcpp::Time now = this->get_clock()->now();
rclcpp::Time when = now - rclcpp::Duration(5, 0);
t = tf_buffer_->lookupTransform(
    toFrameRel,     // 目标帧: turtle2
    now,            // 目标帧时间: 现在
    fromFrameRel,   // 源帧: carrot1
    when,           // 源帧时间: 5秒前
    "world",        // 固定锚点帧
    50ms);          // 超时
```

**六个参数含义**：
1. `toFrameRel` — 要变换到的目标帧（turtle2）
2. `now` — 目标帧的时间（现在）
3. `fromFrameRel` — 源帧（carrot1）
4. `when` — 源帧的时间（5秒前）
5. `"world"` — 不随时间变化的锚点帧
6. `50ms` — 超时等待

底层逻辑：tf2 先在"过去"把 carrot1 变换到 world 帧 → 在 world 帧中进行"时间旅行"到当前 → 在当前把 world 变换到 turtle2。

### 8.4 重新编译运行

```bash
cd ~/ros2_ws
colcon build --packages-select learning_tf2_cpp
ros2 launch learning_tf2_cpp turtle_tf2_fixed_frame_demo_launch.xml
```

现在 turtle2 会正确跟随 5 秒前 carrot1 的位置。

---

## 9. 第8节：Debugging tf2 problems

> 目标：学习系统化调试 tf2 问题。

### 9.1 创建调试用的 listener 副本

```bash
cd ~/ros2_ws/src/learning_tf2_cpp/src
cp turtle_tf2_listener.cpp turtle_tf2_listener_debug.cpp
```

打开 `turtle_tf2_listener_debug.cpp`，做两处修改：

**修改 1** — 将第 67 行（大概位置）：

```cpp
std::string toFrameRel = "turtle2";
```

改为：

```cpp
std::string toFrameRel = "turtle3";
```

**修改 2** — 将 `lookupTransform()` 调用中的 `tf2::TimePointZero` 改为 `this->now()`：

```cpp
try {
    t = tf_buffer_->lookupTransform(
        toFrameRel,
        fromFrameRel,
        this->now());    // 原来是 tf2::TimePointZero
}
```

### 9.2 创建调试用的 launch 文件

创建 `~/ros2_ws/src/learning_tf2_cpp/launch/start_tf2_debug_demo_launch.py`：

```python
from launch import LaunchDescription
from launch_ros.actions import Node

def generate_launch_description():
    return LaunchDescription([
        Node(
            package='turtlesim',
            executable='turtlesim_node',
            name='sim'
        ),
        Node(
            package='learning_tf2_cpp',
            executable='turtle_tf2_broadcaster',
            name='broadcaster1',
            parameters=[{'turtlename': 'turtle1'}]
        ),
        Node(
            package='learning_tf2_cpp',
            executable='turtle_tf2_broadcaster',
            name='broadcaster2',
            parameters=[{'turtlename': 'turtle2'}]
        ),
        Node(
            package='learning_tf2_cpp',
            executable='turtle_tf2_listener_debug',
            name='listener_debug'
        ),
    ])
```

### 9.3 修改 CMakeLists.txt

编辑 `~/ros2_ws/src/learning_tf2_cpp/CMakeLists.txt`，添加：

```cmake
add_executable(turtle_tf2_listener_debug src/turtle_tf2_listener_debug.cpp)
ament_target_dependencies(
  turtle_tf2_listener_debug
  geometry_msgs
  rclcpp
  tf2
  tf2_ros
  turtlesim
)

install(TARGETS
  turtle_tf2_listener_debug
  DESTINATION lib/${PROJECT_NAME})
```

### 9.4 编译并运行

```bash
cd ~/ros2_ws
colcon build --packages-select learning_tf2_cpp
# Windows: colcon build --merge-install --packages-select learning_tf2_cpp
```

运行：

```bash
ros2 launch learning_tf2_cpp start_tf2_debug_demo_launch.py
```

再开一个终端开车：

```bash
ros2 run turtlesim turtle_teleop_key
```

### 9.5 调试步骤

**步骤1 — 用 tf2_echo 检查帧是否存在**

```bash
ros2 run tf2_ros tf2_echo turtle3 turtle1
```

输出会告诉你 `turtle3` 不存在。

**步骤2 — 用 view_frames 查看现有帧**

```bash
ros2 run tf2_tools view_frames
```

打开生成的 `frames.pdf`，看到只有 `turtle1` 和 `turtle2`，没有 `turtle3`。

**修复**：把 `turtle_tf2_listener_debug.cpp` 中的 `turtle3` 改回 `turtle2`。

**步骤3 — 编译运行后遇到下一个问题**

会有 extrapolation into the future 错误。

**步骤4 — 用 tf2_monitor 检查时间延迟**

```bash
ros2 run tf2_ros tf2_monitor turtle2 turtle1
```

可以看到平均延迟约 3ms。说明用 `now` 请求太早了。

**步骤5 — 临时修复（向后回溯 100ms）**

修改 listener：

```cpp
try {
    t = tf_buffer_->lookupTransform(
        toFrameRel,
        fromFrameRel,
        this->now() - rclcpp::Duration::from_seconds(0.1));
}
```

**真正的修复是**：要么用 `tf2::TimePointZero`（取最新可用数据），要么用带 timeout 的版本：

```cpp
try {
    t = tf_buffer_->lookupTransform(
        toFrameRel,
        fromFrameRel,
        this->now(),
        rclcpp::Duration::from_seconds(0.05));
}
```

---

## 10. 第9节：Quaternion fundamentals

> 目标：学习 ROS 2 中四元数的基础知识。

**注意：这一节是理论 + 代码片段，不需要创建完整节点或运行 launch 文件。**

### 10.1 理解四元数分量

ROS 2 的四元数格式为 `(x, y, z, w)`，其中 `w` 在最后。

单位四元数（无旋转）为 `(0, 0, 0, 1)`。

### 10.2 C++ 中创建四元数

```cpp
#include <tf2/LinearMath/Quaternion.h>

tf2::Quaternion q;
q.setRPY(0, 0, 0);  // roll=0, pitch=0, yaw=0
// q 的分量为 (0, 0, 0, 1)
```

### 10.3 C++ 中转换四元数类型

```cpp
#include <tf2_geometry_msgs/tf2_geometry_msgs.hpp>

tf2::Quaternion tf2_quat;
tf2_quat.setRPY(roll, pitch, yaw);

// tf2::Quaternion → geometry_msgs::msg::Quaternion
geometry_msgs::msg::Quaternion msg_quat = tf2::toMsg(tf2_quat);

// geometry_msgs::msg::Quaternion → tf2::Quaternion
tf2::fromMsg(msg_quat, tf2_quat_from_msg);
```

### 10.4 Python 中使用四元数

```python
from geometry_msgs.msg import Quaternion

# Python 中用 list 表示四元数 [x, y, z, w]
quat_tf = [0.0, 1.0, 0.0, 0.0]

# 转换为 geometry_msgs.msg.Quaternion
msg_quat = Quaternion(x=quat_tf[0], y=quat_tf[1], z=quat_tf[2], w=quat_tf[3])
```

### 10.5 常用操作

**从 Euler 角 → 四元数**：

```python
# 使用前面 turtle_tf2_broadcaster.py 中的 quaternion_from_euler 函数
q = quaternion_from_euler(1.5707, 0, -1.5707)
```

**应用旋转（乘法，顺序重要）**：

```cpp
// C++
tf2::Quaternion q_orig, q_rot, q_new;
q_orig.setRPY(0.0, 0.0, 0.0);
q_rot.setRPY(3.14159, 0.0, 0.0);   // 绕 X 轴旋转 180°
q_new = q_rot * q_orig;
q_new.normalize();
```

```python
# Python
q_orig = quaternion_from_euler(0, 0, 0)
q_rot = quaternion_from_euler(3.14159, 0, 0)
q_new = quaternion_multiply(q_rot, q_orig)
```

**逆四元数**：对 x、y、z 取反，w 不变：

```python
q[0] = -q[0]  # x
q[1] = -q[1]  # y
q[2] = -q[2]  # z
# q[3] (w) 不变
```

**相对旋转**：`q_r = q_2 * q_1_inverse`

---

## 11. 第10节：Using stamped datatypes with tf2_ros::MessageFilter

> 目标：使用 `tf2_ros::MessageFilter` 处理带时间戳的消息类型。

### 11.1 安装依赖包

```bash
# Ubuntu
sudo apt install ros-humble-turtle-tf2-py

# RHEL
sudo dnf install ros-humble-turtle-tf2-py

# 从源码安装
cd ~/ros2_ws/src
git clone https://github.com/ros/geometry_tutorials.git -b ros2
cd ~/ros2_ws
colcon build --packages-select turtle_tf2_py
```

### 11.2 创建 Python 广播节点 — 发布 PointStamped 消息

```bash
cd ~/ros2_ws/src/learning_tf2_py/learning_tf2_py
```

下载：

```bash
# Linux / macOS
wget https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/turtle_tf2_message_broadcaster.py

# Windows (cmd)
curl -sk https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/turtle_tf2_message_broadcaster.py -o turtle_tf2_message_broadcaster.py

# Windows (PowerShell)
curl https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_py/turtle_tf2_py/turtle_tf2_message_broadcaster.py -o turtle_tf2_message_broadcaster.py
```

`turtle_tf2_message_broadcaster.py` 内容：

```python
from geometry_msgs.msg import PointStamped
from geometry_msgs.msg import Twist

import rclpy
from rclpy.node import Node

from turtlesim.msg import Pose
from turtlesim.srv import Spawn


class PointPublisher(Node):
    def __init__(self):
        super().__init__('turtle_tf2_message_broadcaster')
        self.spawner = self.create_client(Spawn, 'spawn')
        self.turtle_spawning_service_ready = False
        self.turtle_spawned = False
        self.turtle_pose_cansubscribe = False
        self.timer = self.create_timer(1.0, self.on_timer)

    def on_timer(self):
        if self.turtle_spawning_service_ready:
            if self.turtle_spawned:
                self.turtle_pose_cansubscribe = True
            else:
                if self.result.done():
                    self.get_logger().info(
                        f'Successfully spawned {self.result.result().name}')
                    self.turtle_spawned = True
                else:
                    self.get_logger().info('Spawn is not finished')
        else:
            if self.spawner.service_is_ready():
                request = Spawn.Request()
                request.name = 'turtle3'
                request.x = 4.0
                request.y = 2.0
                request.theta = 0.0
                self.result = self.spawner.call_async(request)
                self.turtle_spawning_service_ready = True
            else:
                self.get_logger().info('Service is not ready')

        if self.turtle_pose_cansubscribe:
            self.vel_pub = self.create_publisher(Twist, 'turtle3/cmd_vel', 10)
            self.sub = self.create_subscription(Pose, 'turtle3/pose', self.handle_turtle_pose, 10)
            self.pub = self.create_publisher(PointStamped, 'turtle3/turtle_point_stamped', 10)

    def handle_turtle_pose(self, msg):
        vel_msg = Twist()
        vel_msg.linear.x = 1.0
        vel_msg.angular.z = 1.0
        self.vel_pub.publish(vel_msg)

        ps = PointStamped()
        ps.header.stamp = self.get_clock().now().to_msg()
        ps.header.frame_id = 'world'
        ps.point.x = msg.x
        ps.point.y = msg.y
        ps.point.z = 0.0
        self.pub.publish(ps)


def main():
    rclpy.init()
    node = PointPublisher()
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    rclpy.shutdown()
```

### 11.3 修改 setup.py — 添加入口点

在 `console_scripts` 中添加：

```python
'turtle_tf2_message_broadcaster = learning_tf2_py.turtle_tf2_message_broadcaster:main',
```

### 11.4 创建传感器的 launch 文件

创建 `~/ros2_ws/src/learning_tf2_py/launch/turtle_tf2_sensor_message_launch.py`：

```python
from launch import LaunchDescription
from launch_ros.actions import Node

def generate_launch_description():
    return LaunchDescription([
        Node(
            package='turtlesim',
            executable='turtlesim_node',
            name='sim'
        ),
        Node(
            package='learning_tf2_py',
            executable='turtle_tf2_broadcaster',
            name='broadcaster1',
            parameters=[{'turtlename': 'turtle1'}]
        ),
        Node(
            package='learning_tf2_py',
            executable='turtle_tf2_message_broadcaster',
            name='message_broadcaster'
        ),
    ])
```

### 11.5 创建 C++ 消息过滤器节点

进入 C++ 包源码目录：

```bash
cd ~/ros2_ws/src/learning_tf2_cpp/src
```

下载：

```bash
# Linux / macOS
wget https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_cpp/src/turtle_tf2_message_filter.cpp

# Windows (cmd)
curl -sk https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_cpp/src/turtle_tf2_message_filter.cpp -o turtle_tf2_message_filter.cpp

# Windows (PowerShell)
curl https://raw.githubusercontent.com/ros/geometry_tutorials/ros2/turtle_tf2_cpp/src/turtle_tf2_message_filter.cpp -o turtle_tf2_message_filter.cpp
```

### 11.6 修改 C++ 包的 package.xml

编辑 `~/ros2_ws/src/learning_tf2_cpp/package.xml`，添加：

```xml
<depend>message_filters</depend>
<depend>tf2_geometry_msgs</depend>
```

### 11.7 修改 CMakeLists.txt

编辑 `~/ros2_ws/src/learning_tf2_cpp/CMakeLists.txt`，在 `find_package` 段添加：

```cmake
find_package(message_filters REQUIRED)
find_package(tf2_geometry_msgs REQUIRED)
```

添加跨版本兼容代码：

```cmake
if(TARGET tf2_geometry_msgs::tf2_geometry_msgs)
  get_target_property(_include_dirs tf2_geometry_msgs::tf2_geometry_msgs INTERFACE_INCLUDE_DIRECTORIES)
else()
  set(_include_dirs ${tf2_geometry_msgs_INCLUDE_DIRS})
endif()

find_file(TF2_CPP_HEADERS
  NAMES tf2_geometry_msgs.hpp
  PATHS ${_include_dirs}
  NO_CACHE
  PATH_SUFFIXES tf2_geometry_msgs
)
```

添加可执行文件：

```cmake
add_executable(turtle_tf2_message_filter src/turtle_tf2_message_filter.cpp)
ament_target_dependencies(
  turtle_tf2_message_filter
  geometry_msgs
  message_filters
  rclcpp
  tf2
  tf2_geometry_msgs
  tf2_ros
)

if(EXISTS ${TF2_CPP_HEADERS})
  target_compile_definitions(turtle_tf2_message_filter PUBLIC -DTF2_CPP_HEADERS)
endif()

install(TARGETS
  turtle_tf2_message_filter
  DESTINATION lib/${PROJECT_NAME})
```

### 11.8 编译

```bash
cd ~/ros2_ws
colcon build --packages-select learning_tf2_py learning_tf2_cpp
# Windows: colcon build --merge-install --packages-select learning_tf2_py learning_tf2_cpp
```

### 11.9 运行

**终端1** — 启动传感器 demo：

```bash
source /opt/ros/humble/setup.bash
cd ~/ros2_ws
. install/setup.bash
ros2 launch learning_tf2_py turtle_tf2_sensor_message_launch.py
```

你会看到 turtlesim 窗口，turtle3 沿圆周运动。

**终端2** — 启动键盘控制（可选）：

```bash
ros2 run turtlesim turtle_teleop_key
```

**终端3** — 查看 turtle3 发布的 PointStamped 数据：

```bash
ros2 topic echo /turtle3/turtle_point_stamped
```

**终端4** — 运行 C++ 消息过滤器节点：

```bash
ros2 run learning_tf2_cpp turtle_tf2_message_filter
```

你会看到类似输出：

```
[INFO] [turtle_tf2_pose_drawer]: Point of turtle3 in frame of turtle1: x:-6.493231 y:-2.961614 z:0.000000
```

说明 `MessageFilter` 成功将 `world` 帧下的 turtle3 位置转换到了 `turtle1` 帧下。

---

## 附录：常用命令速查

| 命令 | 用途 |
|------|------|
| `ros2 run tf2_ros tf2_echo <源帧> <目标帧>` | 查看两帧之间的变换 |
| `ros2 run tf2_tools view_frames` | 生成帧树 PDF（Linux only） |
| `ros2 run tf2_ros tf2_monitor <源帧> <目标帧>` | 查看帧变换的统计和延迟 |
| `ros2 topic echo /tf_static` | 查看发布的静态变换 |
| `ros2 run tf2_ros static_transform_publisher --x --y --z --yaw --pitch --roll --frame-id --child-frame-id` | 命令行发布静态变换 |
| `ros2 pkg create --build-type ament_python <包名>` | 创建 Python 包 |
| `colcon build --packages-select <包名>` | 只编译指定包 |
| `rosdep install -i --from-path src --rosdistro humble -y` | 检查并安装依赖 |

---

*笔记生成时间：2026-06-17*
*基于 ROS2 Humble 官方文档*
